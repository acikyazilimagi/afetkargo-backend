export enum RoleType {
    RECEIVER = 'receiver',
    DRIVER = 'driver',
    USER = 'user',
    ADMIN = 'admin',
}

export enum RoleTypeEnum {
    ADMIN = 1,
    USER = 2, 
    DRIVER = 3,
    RECEIVER = 4
}